═══════════════════════════════════════
 AIRDROP · 2 MIN SCREEN DEMO  (v2)
 paste this into the teleprompter
═══════════════════════════════════════

 WHAT CHANGED FROM v1
   v1 was a good demo and a bad explanation.
   It never said you built a model, never said
   how a parachute can steer at all, and its
   two best moments (the wandering, and the
   W-squared rule) both rested on ideas it
   never taught. All four are fixed below.

 BEFORE YOU HIT RECORD
   · refresh the page (Ctrl+Shift+R)
   · zoom 90%, close other tabs
   · scroll to  01 · THE TOY
   · click the FIRST card: "Nothing at all"
   · DO NOT touch any slider
   · turn CAPTIONS on in the recorder

 HOW TO READ THIS
   Don't read it word for word.
   Glance down, then say it your way.
   "/" is just a breathing point.


───────────────────────────────  0:00
[ say your name / class / school first ]
[ PRESS "drop it" ]
[ SILENCE. 3 seconds. Let it land. ]

That's a box of food dropped
out of a helicopter, /
and nothing's steering it.

It landed twenty-four metres
from where it should have.


───────────────────────────────  0:12
[ stay put · don't scroll ]

In the Assam floods last July, /
eighty thousand people could only
be reached by air.

So I built a model of a parachute
that steers itself.


───────────────────────────────  0:24
        ** HOW IT CAN STEER AT ALL **
[ SCROLL to 02 · IT ISN'T A NORMAL PARACHUTE ]
[ the grey line vs the blue one is already drawn ]
[ optional: press "drop both" to watch it move ]

It's not a round parachute.
It's a parafoil — /
it glides forward,
so pulling one side turns it.

From the moment it's dropped /
it has a fixed amount of flying in it.

Remember that.


───────────────────────────────  0:44
[ CLICK 2nd card: "Works the wind out itself" ]
[ PRESS "drop it" ]
[ SILENCE. 3 seconds. ]

Same wind, same height,
same parachute. /
This one works out the wind
on the way down.

Two point three metres —
and no wind sensor.


───────────────────────────────  1:00
          ** THE IMPORTANT BIT **
[ SCROLL to 03 · NO WIND SENSOR ON BOARD ]
[ CLICK "force it straight" ]

So how does it know the wind?

GPS gives its speed over the ground, /
the maths its speed through the air.
The difference is the wind.

Except that fails
in a straight line. /
A headwind and flying slower
look identical.

Forced straight — /
three metres a second out.

[ CLICK "let it turn" ]

Let it turn — four centimetres.

Because when you turn, the wind
keeps pointing one way and you don't.


───────────────────────────────  1:26
[ SCROLL BACK UP to the first drop ]

Remember that spare flying
it had to lose? /
That's why it wandered.

So it was already turning — /
already doing the thing
that finds the wind.

I got that for free.


───────────────────────────────  1:38
[ PRESS "50 drops at once" ]

One drop could be luck. /
So — fifty, randomised.

Ten metres without the wind,
under four with it.


───────────────────────────────  1:47
[ SCROLL to 06 · THE FINDING ]

Now, the weight. /
Heavier flies faster, so it pushes
into wind. /
But heavier turns wider, and a wide turn
is clumsy near the ground.

Twice the wind needs four times the weight. /
Four times the weight,
four times the turn radius.

So the best you can land
gets four times worse. /
And you pick that weight before you drop.
So you carry it even on a still day.

───────────────────────────────  2:00
                                  END



═══════════════════════════════════════
 DON'T READ THIS PART — reference only
═══════════════════════════════════════

 THE FOUR THINGS A JUDGE MUST LEAVE WITH
   1. She built a model of a self-steering
      relief parachute.
   2. It glides, so it can be steered — but it
      has a fixed flying budget.
   3. It finds the wind with no wind sensor,
      and only turning makes that possible —
      which it was doing anyway.
   4. Accuracy is capped by the square of the
      wind you design for.
   If any one of those is missing, the video
   has not done its job.

 NUMBERS THAT SHOULD APPEAR
   cold open drop ........... 23.9 m
   second drop .............. 2.3 m  (green, 21.6 better)
   forced straight .......... 2.82 m/s
   let it turn .............. 0.04 m/s
   50 drops ................. 9.8 m  vs  3.6 m

   If a number is different, a slider moved.
   Reload, redo the setup, start again.

 NEVER SAY
   · "these are the paper's numbers"  (on screen = 50 drops,
     the paper = a separate 150-drop run)
   · "it beats being told the true wind"  (the intervals
     overlap — it is NOT separated)
   · "nobody has done this before"  (they have — say so first)

 IF YOU RUN LONG
   cut the 50 drops at 1:38.
   NEVER cut 0:24 — it is the setup for both
   the callback and the finding.

 PAGE ORDER NOW MATCHES THE SCRIPT
   01 the toy · 02 how it steers · 03 the wind
   04 the result · 05 how heavy · 06 the finding
   07 design chart · 08 six shapes · 09 write-up
   You only ever scroll DOWN, except the one
   deliberate jump back at 1:26.

 IF ASKED: isn't the one that's TOLD the wind better?
   On the median, a bit — but the error bars overlap,
   so at 150 drops those two aren't actually separated.
   What I can back up: drop for drop it cuts the miss
   by 4.26 metres, and it wins 112 times out of 150.

 IF ASKED: has nobody done this?
   They have. Naval Postgraduate School lands one within
   3 metres. ParaDrone flies these paths on a hobby
   microcontroller. What neither of them publishes is how
   the weight and the wind together set the limit on
   accuracy. That part is mine.

 IF ASKED: is this simulated or real?
   Simulated — it is a mathematical model, and I say so
   in the video. No turbulence, no canopy flexing, no
   servo lag, so the numbers are a best case. The model
   names a test build — 1 m2 canopy, 0.5 to 0.8 kg — so a
   real flight can prove it wrong.
